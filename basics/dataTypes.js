"use strict";   //Treats all JS code as the newer version

let name = "Umesh";
let age = 21;
let loggedIn = true;

// number
// bigInt
// String
// boolean
// null => standalone value
// undefined
// symbol => unique

console.log(typeof(null));  // This returns the "object" as the type